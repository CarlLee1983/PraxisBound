# Verification Result: TST-030

## Checks

* lint: pass — `pnpm run lint (eslint packages --max-warnings=0) exits 0`
* static: pass — `pnpm run typecheck (tsc --build) exits 0 for both packages; tests/typescript-tooling.sh's packed-package-surface check lists packages/core/src/review/readiness-sidecar.{d.ts,js} and packages/cli/src/review-readiness.{d.ts,js}/review-readiness-digests.{d.ts,js} in the documented tarball contents, and the CLI help text lists review readiness-digests`
* unit: pass — `node --test packages/core/test/*.test.mjs packages/cli/test/*.test.mjs: 843 tests, 842 pass, 0 fail, 1 skipped by design (pre-existing 4 MiB perf smoke gated behind PRAXISBOUND_PERF_SMOKE=1, unrelated to this Story)`
* integration: pass — `packages/core/test/review-readiness-sidecar.test.mjs (23 tests) drives parseReadinessSidecar/checkReadinessSidecarConsistency/checkReadinessSidecarReferences/buildReadinessOutputOwners/transitiveDependencyClosureDirectories directly; packages/cli/test/review-readiness.test.mjs (22 tests) drives review index/render/preflight/readiness-digests against isolated temporary git repositories built by review-import-respond-support.mjs, including an injectable-filesystem test for a mid-rename failure`
* contract: pass — `docs/typescript-tooling/cli-contract.md documents review readiness-digests (command mapping table, its own contract section, and the corrected atomicity/mode/post-rewrite-fingerprint behavior), the Readiness Sidecar's exemption from the generic 4 MiB cap in review index/render, the unreadable-but-present handling, and every REVIEW_READINESS_* code, the field-path-only message rule, and REVIEW_INPUT_TOO_LARGE's shared INCOMPLETE class in the review preflight section, matching what the tests assert`
* e2e: pass — `the built packages/cli/dist/bin.js ran on a scratch batch in an isolated temporary git repository (never this repository's own Stories): review index listed the Sidecar as a source; review preflight on a placeholder-digest Sidecar returned REVIEW_BLOCKED/REVIEW_READINESS_INVALID; review readiness-digests rewrote only the two digest fields (2-space indent, trailing newline, original key order and criteria content unchanged) and reported them in data.updated; a confirmed batch with a Semantic Report and the now-current Sidecar returned REVIEW_READY — see Rehearsal below (this rehearsal predates the code-review fixes below and is retained as historical evidence that the base wiring works; the fixes themselves are proven by the named unit/integration tests, not re-rehearsed against the binary)`
* architecture: blocked — `this diff has not had an independent code/security review round completed to acceptance in this session; a first round found the 3 HIGH and several MEDIUM/LOW findings recorded below, all now fixed and tested. Human Review by carl approved the TST-030 Story itself for execution on 2026-09-23 (recorded in the Story's Dependencies), which is authorization to implement, not acceptance of this implementation. Blocked on a human/reviewer action outside this session, not on any failing check.`

## Code Review Follow-up (2026-09-23)

Every finding below was reproduced against the built CLI before the fix, using fixtures under the reviewer's own scratchpad; each fix is TDD'd with a new, named, failing-then-passing test.

* `HIGH-1` (issue messages echoed Sidecar content, `readiness-sidecar.ts` old lines 551/677/691/701/712): fixed by rewriting every `checkReadinessSidecarReferences` message and the `story_ref` mismatch message to name only a field path and array index, never a criterion id, gate id, output id, or any other Sidecar-authored string. Tests: `packages/core/test/review-readiness-sidecar.test.mjs` — `"HIGH-1: a wrong story_ref never echoes the Sidecar's own (attacker-controlled) story_ref value"`, `"HIGH-1: every checkReadinessSidecarReferences message names only a field path and index, never Sidecar content"`; `packages/cli/test/review-readiness.test.mjs` — `"AC-006: a hostile, duplicated output id blocks preflight without leaking into issues, human output, or records"`, `"AC-006: a hostile story_ref never appears in the invalid-Sidecar issue message, human output, or records"`.
* `HIGH-2` (render left a raw bidi/hidden code point, `render-source.ts:415-428`): fixed by reusing `escapeEvidenceProse`/`escapeEvidenceField` (`render-evidence.ts`, the same helpers the evidence area uses, sharing `isHiddenOrReorderingCodePoint`) instead of the plain `escapeHtml`. Tests: `packages/cli/test/review-readiness.test.mjs` — `"AC-006: a hostile, duplicated output id blocks preflight without leaking into issues, human output, or records"` and the retained `"AC-006: hostile Sidecar text is preserved and escaped, never changes the outcome, and is never echoed in issue messages"` (asserts the escaped form appears and the raw `<script>` element does not); ESC/bidi code points are covered by the hostile fixtures in both the Core reference tests above and the CLI duplicate-output-id/story_ref tests, which assert their absence from the envelope, human output, and the written Preflight Report.
* `HIGH-3` (an over-4-MiB Sidecar made preflight `configuration-error`/exit 2, and made index/render fail outright): fixed by exempting Readiness Sidecar paths from the generic 4 MiB per-source cap in both `review.ts`'s `findOversizedSourcePath` call and `index.ts`'s own too-large loop; `index`/`render` now read and hash a Sidecar at whatever size it is and stay `success`; `review preflight`/`review readiness-digests` still reject an over-1-MiB Sidecar via Core's own `parseReadinessSidecar` byte-length check, reporting `REVIEW_INPUT_TOO_LARGE` with the `REVIEW_INCOMPLETE`/exit-1 outcome (never `configuration-error`). Test: `packages/cli/test/review-readiness.test.mjs` — `"HIGH-3: a Sidecar well over 4 MiB still leaves review index/render success; preflight is REVIEW_INCOMPLETE, exit 1, never configuration-error"`.
* `MEDIUM` duplicate criterion/input ids and duplicate JSON keys: fixed with a hand-written recursive-descent JSON parser (`parseJsonRejectingDuplicateKeys`) that rejects any duplicate object key at any depth, plus index-tracked duplicate-id rejection for `criteria[]`/`inputs[]` in `validateSidecarShape`, all as `REVIEW_READINESS_INVALID`. Tests: `packages/core/test/review-readiness-sidecar.test.mjs` — `"MEDIUM: a duplicate criterion id is rejected as invalid, never echoing the id"`, `"MEDIUM: a duplicate input id is rejected as invalid"`, `"MEDIUM: a duplicate JSON key anywhere in the document is rejected as invalid"`; `packages/cli/test/review-readiness.test.mjs` — `"MEDIUM: duplicate criterion ids and duplicate input ids are rejected end to end as REVIEW_READINESS_INVALID"`.
* `MEDIUM` criteria compared as a list, not a set: fixed by comparing `criteria[].id` against `acceptance.md`'s AC id list positionally (same length, same id at each index), not via `Set` equality. Test: `packages/core/test/review-readiness-sidecar.test.mjs` — `"MEDIUM: criteria compared as an ordered list — same ids, wrong order, is a mismatch"`.
* `MEDIUM` a present-but-unreadable Sidecar must not count as absent: fixed with a new `SourceObservation` kind, `unreadable` (distinct from `missing`/ENOENT), produced by `review.ts`'s `readSourceObservation` on any non-ENOENT failure; `index.ts` includes an `unreadable` Readiness Sidecar in `sources` with `sha256: null` and a `REVIEW_SOURCE_MISSING` diagnostic (mirroring contract §4's missing-source handling), and `review-readiness.ts`'s `loadBatchReadinessSidecars` synthesizes a `REVIEW_READINESS_INVALID` parse failure for it so `review preflight`/`review readiness-digests` both resolve it to a definite, non-`READY` outcome. Test: `packages/cli/test/review-readiness.test.mjs` — `"MEDIUM: a present but unreadable readiness.json is not absent — it joins sources with sha256:null and blocks preflight with REVIEW_READINESS_INVALID"`.
* `MEDIUM` `readiness-digests` atomicity: fixed by staging every rewrite as a temporary file first (a single staging failure deletes every temp and writes nothing), re-checking each Sidecar's on-disk bytes against what was validated immediately before renaming (a mismatch aborts the whole run with nothing written), renaming one at a time and reporting exactly the files already renamed in `data.updated` if a rename fails partway, and preserving each original file's mode explicitly (`chmod` after staging, before rename) rather than leaving the temp's own restrictive mode. Tests: `packages/cli/test/review-readiness.test.mjs` — `"MEDIUM: readiness-digests writes nothing when a rename fails partway through, and reports exactly the files already renamed"` (via an injectable `ReadinessDigestsFilesystem.rename`), `"MEDIUM: readiness-digests preserves the original file mode instead of leaving it at 0600"`.
* `MEDIUM` post-rewrite fingerprint/sources: fixed by re-reading the batch once more after every rename succeeds and returning that state's `fingerprint`/`sources`, never the pre-rewrite ones. Test: `packages/cli/test/review-readiness.test.mjs` — `"MEDIUM: after a successful rewrite, data.fingerprint and sources reflect the post-rewrite state"`.
* `MEDIUM` AC-005 byte-identical output: fixed by making `StoryIndex.readinessPath`/`readinessPresent` genuinely optional and only spread into the returned object when a Sidecar is present, so a batch with none omits both keys entirely rather than emitting them as `undefined`/`false`. Test: `packages/cli/test/review-readiness.test.mjs` — `"AC-005: a batch without any Sidecar produces byte-identical review index JSON to before this Story (no readiness keys anywhere)"` (asserts the keys are absent via `in` and that the full serialized `data` contains no `/readiness/i` substring at all).
* Deviation (b) reversed: `review readiness-digests` now reports `REVIEW_INPUT_TOO_LARGE` (not `REVIEW_READINESS_INVALID`) for an over-1-MiB Sidecar, following contract §13 literally; the original verification.md's deviation note is withdrawn (see `AC-004`/`HIGH-3` evidence below). Deviation (a) (the generic 4 MiB index/render cap) is superseded by the `HIGH-3` fix above: there is no longer any size cap on `readiness.json` in `index`/`render` at all, only Core's own 1 MiB check where the content is evaluated.
* `LOW` duplicate outputs reported once per id: fixed in `checkReadinessSidecarReferences` by tracking already-reported output ids and skipping repeats. Test: `packages/core/test/review-readiness-sidecar.test.mjs` — `"LOW: a duplicated output id is reported once per id, not once per occurrence"`.
* `LOW` `readAuthority`/`readTaskMode` issues surfaced: fixed by pushing their returned issues into `computeReadinessPreflightFindings`'s own findings as their own `STORY_*` codes instead of discarding them. Test: `packages/cli/test/review-readiness.test.mjs` — `"LOW: readAuthority/readTaskMode governance issues are surfaced as their own STORY_* preflight findings, not discarded"`.

## Evidence

* `AC-001`: pass — `packages/cli/test/review-readiness.test.mjs: a present, current, consistent readiness.json is listed in review index's data.sources with its own sha256; review render shows "Readiness Sidecar", the Sidecar's own field values (e.g. runner_worker), and its path in the published HTML; a confirmed batch with a valid Semantic Report and that Sidecar yields REVIEW_READY. Rehearsed again end to end against the built CLI binary (see Rehearsal)`
* `AC-002`: pass — `packages/cli/test/review-readiness.test.mjs: readiness-digests on a Sidecar with a stale story_md_digest rewrites both digest fields (2-space indent, trailing newline, unchanged criteria/key order), reports the rewritten path in data.updated, leaves an already-current Sidecar unwritten on a second run (data.updated: []), and afterwards review preflight against the pre-rewrite confirmation's fingerprint returns REVIEW_STALE with REVIEW_CONFIRMATION_STALE`
* `AC-003`: pass — `packages/core/test/review-readiness-sidecar.test.mjs: checkReadinessSidecarConsistency/checkReadinessSidecarReferences yield REVIEW_READINESS_STALE for a digest mismatch, REVIEW_READINESS_CRITERIA_MISMATCH for a criteria list differing from acceptance.md's ordered AC ids (by content or by order), REVIEW_READINESS_OPERATION_UNGRANTED for an ungranted operation and for a runner_worker operation outside plan/modify (even when the Story's Authority itself grants it), REVIEW_READINESS_REFERENCE_UNKNOWN for a prerequisite_story_ref outside the dependency closure, a prerequisite_output owned outside the closure, a follow_up_story_ref outside the batch, and a duplicate outputs[].id across two Stories (reported once, per the LOW fix above); human/external-owner criteria produce no operation finding even with an otherwise-ungranted operation. Every reference-check message names only a field path and index (see HIGH-1 above). packages/cli/test/review-readiness.test.mjs confirms the same REVIEW_READINESS_CRITERIA_MISMATCH end to end through review preflight`
* `AC-004`: pass — `packages/core/test/review-readiness-sidecar.test.mjs: an unknown-field or bad-enum Sidecar, a wrong story_ref, a duplicate criterion id, a duplicate input id, and a duplicate JSON key each yield parseReadinessSidecar ok:false with tooLarge:false; a Sidecar whose serialized bytes exceed 1 MiB yields ok:false with tooLarge:true. packages/cli/test/review-readiness.test.mjs confirms end to end: a schema-invalid Sidecar yields REVIEW_BLOCKED/REVIEW_READINESS_INVALID from review preflight; an over-limit (1048577-byte-class, and separately a >5 MiB-class) Sidecar yields REVIEW_INCOMPLETE/REVIEW_INPUT_TOO_LARGE, never REVIEW_READY, never configuration-error (Human Review 2026-09-23: this shared code keeps its existing INCOMPLETE class); readiness-digests fails and writes nothing for a schema-invalid Sidecar (REVIEW_READINESS_INVALID) and for an over-limit one (REVIEW_INPUT_TOO_LARGE, deviation (b) reversed); an unreadable-but-present Sidecar is also REVIEW_READINESS_INVALID (MEDIUM fix above)`
* `AC-005`: pass — `packages/cli/test/review-readiness.test.mjs: a batch fixture with no readiness.json has no readiness.json entry in review index's data.sources and resolves its Story/acceptance normally, and its data.stories[] entries carry neither readinessPath nor readinessPresent at all (asserted via the in operator and a full-JSON /readiness/i sweep); writing a valid readiness.json for the same Story changes the fingerprint and adds it to data.sources. The full pre-existing core/cli test suite (842 of 843 tests, the other skipped by design) passes unchanged after this Story's changes, so every batch without a Sidecar keeps its prior index/fingerprint/render/preflight behavior byte for byte`
* `AC-006`: pass — `packages/cli/test/review-readiness.test.mjs: (1) a Sidecar carrying authorized: true, an instruction, and a <script> tag in a declaration id that is otherwise fully consistent still reaches REVIEW_READY, with the escaped form in the rendered HTML and the raw <script> element absent; (2) a hostile duplicated output id (script tag, ESC, bidi override) across two Stories blocks preflight (REVIEW_READINESS_REFERENCE_UNKNOWN) with none of the hostile substrings appearing in the envelope, the human-rendered preflight output, or the written Preflight Report; (3) a hostile story_ref (script tag, bidi override) blocks preflight (REVIEW_READINESS_INVALID) with the same three checks; a readiness.json symlinked to a file outside the repository yields configuration-error/REVIEW_PATH_UNSAFE from both review index and review preflight, before any content is read, and writes no records/ file`
* `AC-007`: pass — `make verify exits 0 (full composed gate: protocol, bootstrap, doctor, story, handoff, release, typescript, go, actions, execution, tooling, praxisbound); docs/typescript-tooling/cli-contract.md and the CLI help text list review readiness-digests and every REVIEW_READINESS_* code, including the code-review corrections above; VERSION, protocol/, and templates/ are byte-identical to origin/main (git diff --stat origin/main -- VERSION protocol/ templates/ is empty); TST-021 through TST-028's own test files pass unchanged (842 of 843 total core+cli tests, 1 pre-existing skip)`

## Rehearsal

One real, manually driven run of the built `packages/cli/dist/bin.js` against a
scratch batch in an isolated temporary git repository (never this
repository's own Stories, handoff, or work tree), recorded here as
Historical Evidence that the wiring works end to end. This rehearsal predates
the code-review round above; it demonstrates the base command wiring, not the
fixes themselves, which are proven by the named tests above instead of a
second rehearsal:

* `review index <manifest> --json`: a Story with no readiness.json lists 4 sources (no readiness.json entry); adding readiness.json with a placeholder digest lists 5 sources including it, with its own sha256
* `review preflight <manifest> --json` (placeholder-digest Sidecar, no confirmation/report): `REVIEW_BLOCKED`, exit 1, `REVIEW_READINESS_INVALID` ("story_md_digest has an invalid form") alongside `REVIEW_CONFIRMATION_MISSING`/`REVIEW_SEMANTIC_MISSING`
* `review readiness-digests <manifest> --json` (schema-invalid placeholder): `failure`, exit 1, `REVIEW_READINESS_INVALID`, no file changed
* readiness.json edited to a well-formed but stale `sha256:00...00` digest; `review readiness-digests <manifest> --json`: `success`, exit 0, `data.updated` names the Sidecar path; the file's `story_md_digest`/`acceptance_md_digest` now equal the current `story.md`/`acceptance.md` bytes, `criteria`/`inputs`/`outputs`/`decision_follow_ups` and key order unchanged, 2-space indent, one trailing newline
* A Definition Confirmation and Semantic Report written for the post-rewrite fingerprint; `review preflight <manifest> --semantic-report semantic-report.json --json`: `REVIEW_READY`, exit 0, no diagnostics

## Authority Used

* plan
* modify
* commit

## Residual Risks

* `This diff has not yet had an independent code/security review round completed to full acceptance in this session; the first round's findings are all fixed and tested above, but a second, confirming review has not run.`
* `The Readiness Sidecar's own consistency checks (digest, criteria, operations, references) validate agreement with the Story and batch only; they never verify that a declaration is true, exactly as contract §21 states — a Sidecar can still declare something false that is internally consistent.`
* `review readiness-digests re-reads and re-validates every present Sidecar on every run (no incremental cache), and now re-reads the whole batch a second time after a successful rewrite to report the post-rewrite fingerprint; this is bounded by contract §13's 200-Sidecar-per-batch limit and matches the existing review preflight/index cost model, but a very large batch pays for two full passes.`
* `A Readiness Sidecar has no size cap at all in review index/review render (HIGH-3 fix): an arbitrarily large one is read fully into memory to compute its digest, bounded only by available memory and the batch's 200-Sidecar/200-Story limits, not by any byte cap the way other declared sources are. This is the literal, requested behavior (contract §13/§21's 1 MiB bound applies only where the content is evaluated), recorded here as the accepted trade-off, not an oversight.`
* `The hand-written recursive-descent JSON parser (parseJsonRejectingDuplicateKeys) duplicates JSON.parse's grammar for readiness.json only; it is exercised by the duplicate-key test and by every other parseReadinessSidecar test (which all now go through it), but it is new code with its own edge cases (e.g. surrogate-pair \u escapes) that a hand-rolled parser could still get wrong in a case not covered by the current fixtures.`
* `The mid-rename-failure test drives runReviewReadinessDigests through an injectable ReadinessDigestsFilesystem.rename rather than a real filesystem-level fault (e.g. a real EACCES on the second rename); this proves the code path deterministically but does not prove every real-world rename failure mode reaches it identically.`
