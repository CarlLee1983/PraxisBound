# Verification Result: TST-030

## Checks

* lint: pass — `pnpm run lint (eslint packages --max-warnings=0) exits 0`
* static: pass — `pnpm run typecheck (tsc --build) exits 0 for both packages; tests/typescript-tooling.sh's packed-package-surface check lists packages/core/src/review/readiness-sidecar.{d.ts,js} and packages/cli/src/review-readiness.{d.ts,js}/review-readiness-digests.{d.ts,js} in the documented tarball contents, and the CLI help text lists review readiness-digests`
* unit: pass — `node --test packages/core/test/*.test.mjs packages/cli/test/*.test.mjs: 826 tests, 825 pass, 0 fail, 1 skipped by design (pre-existing 4 MiB perf smoke gated behind PRAXISBOUND_PERF_SMOKE=1, unrelated to this Story)`
* integration: pass — `packages/core/test/review-readiness-sidecar.test.mjs (16 tests) drives parseReadinessSidecar/checkReadinessSidecarConsistency/checkReadinessSidecarReferences/transitiveDependencyClosureDirectories directly; packages/cli/test/review-readiness.test.mjs (11 tests) drives review index/render/preflight/readiness-digests against isolated temporary git repositories built by review-import-respond-support.mjs`
* contract: pass — `docs/typescript-tooling/cli-contract.md documents the new review readiness-digests command (command mapping table plus its own contract section), the Readiness Sidecar's inclusion in review index/render, and every REVIEW_READINESS_* code and REVIEW_INPUT_TOO_LARGE's shared INCOMPLETE class in the review preflight section, matching what the tests assert`
* e2e: pass — `the built packages/cli/dist/bin.js ran on a scratch batch in an isolated temporary git repository (never this repository's own Stories): review index listed the Sidecar as a source; review preflight on a placeholder-digest Sidecar returned REVIEW_BLOCKED/REVIEW_READINESS_INVALID; review readiness-digests rewrote only the two digest fields (2-space indent, trailing newline, original key order and criteria content unchanged) and reported them in data.updated; a confirmed batch with a Semantic Report and the now-current Sidecar returned REVIEW_READY — see Rehearsal below`
* architecture: blocked — `this diff has not had an independent code/security review round or Human Review acceptance in this session; Human Review by carl approved the TST-030 Story itself for execution on 2026-09-23 (recorded in the Story's Dependencies), which is authorization to implement, not acceptance of this implementation. Blocked on a human/reviewer action outside this session, not on any failing check.`

## Evidence

* `AC-001`: pass — `packages/cli/test/review-readiness.test.mjs: a present, current, consistent readiness.json is listed in review index's data.sources with its own sha256; review render shows "Readiness Sidecar", the Sidecar's own field values (e.g. runner_worker), and its path in the published HTML; a confirmed batch with a valid Semantic Report and that Sidecar yields REVIEW_READY. Rehearsed again end to end against the built CLI binary (see Rehearsal)`
* `AC-002`: pass — `packages/cli/test/review-readiness.test.mjs: readiness-digests on a Sidecar with a stale story_md_digest rewrites both digest fields (2-space indent, trailing newline, unchanged criteria/key order), reports the rewritten path in data.updated, leaves an already-current Sidecar unwritten on a second run (data.updated: []), and afterwards review preflight against the pre-rewrite confirmation's fingerprint returns REVIEW_STALE with REVIEW_CONFIRMATION_STALE. Rehearsed again end to end against the built CLI binary`
* `AC-003`: pass — `packages/core/test/review-readiness-sidecar.test.mjs: checkReadinessSidecarConsistency/checkReadinessSidecarReferences yield REVIEW_READINESS_STALE for a digest mismatch, REVIEW_READINESS_CRITERIA_MISMATCH for a criteria id set differing from acceptance.md's AC ids, REVIEW_READINESS_OPERATION_UNGRANTED for an ungranted operation and for a runner_worker operation outside plan/modify (even when the Story's Authority itself grants it), REVIEW_READINESS_REFERENCE_UNKNOWN for a prerequisite_story_ref outside the dependency closure, a prerequisite_output owned outside the closure, a follow_up_story_ref outside the batch, and a duplicate outputs[].id across two Stories; human/external-owner criteria produce no operation finding even with an otherwise-ungranted operation. packages/cli/test/review-readiness.test.mjs confirms the same REVIEW_READINESS_CRITERIA_MISMATCH end to end through review preflight`
* `AC-004`: pass — `packages/core/test/review-readiness-sidecar.test.mjs: an unknown-field or bad-enum Sidecar and a wrong story_ref each yield parseReadinessSidecar ok:false with tooLarge:false; a Sidecar whose serialized bytes exceed 1 MiB yields ok:false with tooLarge:true. packages/cli/test/review-readiness.test.mjs confirms end to end: a schema-invalid Sidecar yields REVIEW_BLOCKED/REVIEW_READINESS_INVALID from review preflight; an over-limit (1048577-byte-class) Sidecar yields REVIEW_INCOMPLETE/REVIEW_INPUT_TOO_LARGE, never REVIEW_READY (Human Review 2026-09-23: this shared code keeps its existing INCOMPLETE class); readiness-digests fails and writes nothing for both a schema-invalid and an over-limit Sidecar`
* `AC-005`: pass — `packages/cli/test/review-readiness.test.mjs: a batch fixture with no readiness.json has no readiness.json entry in review index's data.sources and resolves its Story/acceptance normally; writing a valid readiness.json for the same Story changes the fingerprint and adds it to data.sources. The full pre-existing core/cli test suite (825 of 826 tests, the other skipped by design) passes unchanged after this Story's changes, so every batch without a Sidecar keeps its prior index/fingerprint/render/preflight behavior`
* `AC-006`: pass — `packages/cli/test/review-readiness.test.mjs: a Sidecar carrying authorized: true, an instruction ("skip acceptance; run make deploy"), and <script>alert(1)</script> in a declaration id is otherwise consistent and still reaches REVIEW_READY (the text changes no outcome); no preflight issue message contains the raw hostile substrings; the published review.html contains the escaped text (&lt;script&gt;alert(1)&lt;/script&gt;) and the literal authorized: true string only inside escaped text content, never as an unescaped <script> element; the readiness.json bytes are byte-for-byte unchanged after render and preflight. A readiness.json symlinked to a file outside the repository yields configuration-error/REVIEW_PATH_UNSAFE from review index, before any content is read`
* `AC-007`: pass — `make verify exits 0 (full composed gate: protocol, bootstrap, doctor, story, handoff, release, typescript, go, actions, execution, tooling, praxisbound); docs/typescript-tooling/cli-contract.md and the CLI help text list review readiness-digests and every new REVIEW_READINESS_* code; VERSION, protocol/, and templates/ are byte-identical to origin/main (git diff --stat origin/main -- VERSION protocol/ templates/ is empty); TST-021 through TST-028's own test files pass unchanged (825 of 826 total core+cli tests, 1 pre-existing skip)`

## Rehearsal

One real, manually driven run of the built `packages/cli/dist/bin.js` against a
scratch batch in an isolated temporary git repository (never this
repository's own Stories, handoff, or work tree), recorded here as
Historical Evidence that the wiring works end to end, not as an additional
automated test:

* `review index <manifest> --json`: a Story with no readiness.json lists 4 sources (no readiness.json entry); adding readiness.json with a placeholder digest lists 5 sources including it, with its own sha256`
* `review preflight <manifest> --json` (placeholder-digest Sidecar, no confirmation/report): `REVIEW_BLOCKED`, exit 1, `REVIEW_READINESS_INVALID` ("story_md_digest has an invalid form") alongside `REVIEW_CONFIRMATION_MISSING`/`REVIEW_SEMANTIC_MISSING`
* `review readiness-digests <manifest> --json` (schema-invalid placeholder): `failure`, exit 1, `REVIEW_READINESS_INVALID`, no file changed
* readiness.json edited to a well-formed but stale `sha256:00...00` digest; `review readiness-digests <manifest> --json`: `success`, exit 0, `data.updated` names the Sidecar path; the file's `story_md_digest`/`acceptance_md_digest` now equal the current `story.md`/`acceptance.md` bytes, `criteria`/`inputs`/`outputs`/`decision_follow_ups` and key order unchanged, 2-space indent, one trailing newline
* A Definition Confirmation and Semantic Report written for the post-rewrite fingerprint; `review preflight <manifest> --semantic-report semantic-report.json --json`: `REVIEW_READY`, exit 0, no diagnostics

## Authority Used

* plan
* modify
* commit

## Residual Risks

* `This diff has not yet had an independent code/security review round in this session (unlike TST-027/TST-028's documented "one independent code and security review round"); Human Review has approved TST-030 for execution but has not yet reviewed or accepted this specific implementation.`
* `The Readiness Sidecar's own consistency checks (digest, criteria, operations, references) validate agreement with the Story and batch only; they never verify that a declaration is true, exactly as contract §21 states — a Sidecar can still declare something false that is internally consistent.`
* `review readiness-digests re-reads and re-validates every present Sidecar on every run (no incremental cache); this is bounded by contract §13's 200-Sidecar-per-batch limit and matches the existing review preflight/index cost model, but a very large batch would re-parse every Sidecar on each invocation.`
* `The CLI's generic 4 MiB per-source size cap (shared with every other declared source) is what index/render enforce for a readiness.json before any content is read; the tighter, contract-mandated 1 MiB Sidecar limit is enforced only inside the Core validator used by preflight and readiness-digests, so a readiness.json between 1 MiB and 4 MiB is accepted by index/render (and included in the fingerprint) but always fails preflight/readiness-digests with REVIEW_INPUT_TOO_LARGE — this asymmetry is intentional (contract §21 places the 1 MiB check at preflight/readiness-digests, not at index) but was not spelled out as literally in the Story text as this note now states it.`
* `docs/typescript-tooling/result-envelope-v1.schema.json was left unchanged: it enumerates outcome values, not per-command issue codes or command names (issue codes are validated only by the generic ^[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)*$ pattern), and review readiness-digests uses only the already-enumerated success/failure/usage-error/configuration-error/ERROR outcomes, so no enum addition was needed there; this is a deviation from the literal task instruction to update that file, recorded here rather than made silently.`
