# Verification Result: TST-030

## Checks

* lint: pass — `pnpm run lint (eslint packages --max-warnings=0) exits 0`
* static: pass — `pnpm run typecheck (tsc --build) exits 0 for both packages; tests/typescript-tooling.sh's packed-package-surface check lists packages/core/src/review/{readiness-sidecar,json-safety}.{d.ts,js} and packages/cli/src/review-readiness.{d.ts,js}/review-readiness-digests.{d.ts,js} in the documented tarball contents, and the CLI help text lists review readiness-digests`
* unit: pass — `node --test packages/core/test/*.test.mjs packages/cli/test/*.test.mjs: 850 tests, 849 pass, 0 fail, 1 skipped by design (pre-existing 4 MiB perf smoke gated behind PRAXISBOUND_PERF_SMOKE=1, unrelated to this Story)`
* integration: pass — `packages/core/test/review-readiness-sidecar.test.mjs (24 tests) drives parseReadinessSidecar/checkReadinessSidecarConsistency/checkReadinessSidecarReferences/buildReadinessOutputOwners/transitiveDependencyClosureDirectories directly; packages/cli/test/review-readiness.test.mjs (27 tests) drives review index/render/preflight/readiness-digests against isolated temporary git repositories built by review-import-respond-support.mjs, including an injectable-filesystem test for a mid-rename failure`
* contract: pass — `docs/typescript-tooling/cli-contract.md documents review readiness-digests (command mapping table, its own contract section, and the corrected atomicity/mode/pre-stage-symlink-check/post-rewrite-fingerprint/updated-on-reindex-failure behavior), the Readiness Sidecar's exemption from the generic 4 MiB cap and its fstat-before-read/streamed-digest/oversized handling in review index/render, the unreadable-but-present handling with its distinct message, the __proto__-safe JSON.parse path via json-safety.ts, and every REVIEW_READINESS_* code, the field-path-only message rule, and REVIEW_INPUT_TOO_LARGE's shared INCOMPLETE class in the review preflight section, matching what the tests assert`
* e2e: pass — `the built packages/cli/dist/bin.js was rehearsed twice: once (pre-existing, historical) on a scratch batch showing the base command wiring — see Rehearsal below; once (this round) against the code reviewer's own repro fixtures under the reviewer's scratchpad r2/ directory (never this repository's own Stories or work tree) — see Round-2 Rehearsal below, run under a constrained 512 MiB V8 heap (--max-old-space-size=512) against the reviewer's 268 MB huge/readiness.json and confirming no crash, a correctly streamed sha256, and REVIEW_INPUT_TOO_LARGE from every command that evaluates it`
* architecture: blocked — `this diff has had two code-review rounds in this session (3 HIGH + several MEDIUM/LOW in round 1, all fixed; 2 HIGH + 1 MEDIUM + 3 LOW in round 2, all fixed below) but no round has yet reached full acceptance. Human Review by carl approved the TST-030 Story itself for execution on 2026-09-23 (recorded in the Story's Dependencies), which is authorization to implement, not acceptance of this implementation. Blocked on a human/reviewer action outside this session, not on any failing check.`

## Code Review Follow-up — Round 2 (2026-09-23)

Every finding below was reproduced against the built CLI before the fix, using the reviewer's own fixtures (`.../scratchpad/r2/{hostile,proto,huge,big,mid,unr,rd,none}`); each fix is TDD'd with a new, named, failing-then-passing test, and the two size/prototype fixes are additionally re-verified against the reviewer's real fixtures with the built binary (see Round-2 Rehearsal).

* `HIGH-1` (a Sidecar had no size bound and was read whole into memory — `review.ts:599`, `render.ts:562`; a 256 MiB file OOM-crashed `render` with no envelope; a 5 MiB one produced 10.5 MB of HTML from double-printing): fixed with a dedicated reader, `readReadinessSidecarObservation` (`packages/cli/src/review.ts`), that checks size via `fstat` on the already-`O_NOFOLLOW`-opened handle *before* reading; at or under the §13/§21 1 MiB bound it reads and hashes normally; over that bound it streams the bytes through a `sha256` hash (`handle.createReadStream`) without ever holding them in memory, and returns a new `SourceObservation` kind, `oversized`, carrying that digest. `indexReviewBatch` includes an `oversized` Sidecar in `sources`/the fingerprint using that digest directly (no bytes needed); `review render` shows a fixed size notice (`renderReadinessSidecarOversizedNoticeHtml`) in its place, never content; `review preflight`/`review readiness-digests` (`loadBatchReadinessSidecars`) report it as `tooLarge: true` (`REVIEW_INPUT_TOO_LARGE`) without ever calling `parseReadinessSidecar`. The double-print half of this finding is fixed by excluding every Readiness Sidecar path from the appendix's raw-source dump (see `MEDIUM-1`), so its content is never printed more than once regardless of size. Tests: `packages/cli/test/review-readiness.test.mjs` — `"HIGH-1: an over-1-MiB Sidecar is never parsed or rendered; review index/render stay success and show only a size notice"` (3+ MiB, asserts a marker embedded past the 1 MiB mark never reaches the rendered HTML, the streamed digest matches the real file bytes exactly, and the HTML stays under 1 MiB despite the input being 3+ MiB). Re-verified against the reviewer's real 268 MB `huge/` fixture with the built binary under a 512 MiB heap cap — see Round-2 Rehearsal.
* `HIGH-2` (the hand-written JSON parser let `"__proto__"` repoint the parsed object's prototype — `obj[key] = parseValue()` — so preflight reported only `STALE` and `readiness-digests` silently dropped the author's content): fixed by deleting the hand-written recursive-descent parser entirely and reusing the existing `scanJsonSafety` approach (duplicate-key and depth scan over the raw text, *then* the real `JSON.parse`) — copied, not imported, into a new shared module `packages/core/src/review/json-safety.ts` (not editing `goal-plan-artifacts.ts`, which TST-029 is rewriting on another branch, per instruction — the two are to be deduplicated after both land). `JSON.parse` defines each property directly rather than through `[[Set]]`, so an authored `"__proto__"` key becomes an ordinary own data property, which the existing `unknownKey` schema check then rejects as an unrecognized field. The misleading doc comment on `ReadinessSidecarParseResult.raw` (claiming "the exact `JSON.parse` result" when round 1's `raw` actually came from the hand-written parser) is now accurate again and updated to say so explicitly. Tests: `packages/core/test/review-readiness-sidecar.test.mjs` — `"HIGH-2: a __proto__ key (top-level and inside a criterion) is rejected as an unknown field, never repointing the object's prototype"` (asserts `({}).polluted` and `Object.prototype.polluted` stay `undefined`); `packages/cli/test/review-readiness.test.mjs` — `"HIGH-2: a __proto__ Sidecar blocks preflight as REVIEW_READINESS_INVALID (never REVIEW_STALE, never Object.prototype pollution)"`, `"HIGH-2: readiness-digests fails and never drops content for a __proto__ Sidecar"` (asserts the file's bytes are byte-for-byte unchanged after the failed run). Re-verified against the reviewer's real `dupkey/` fixture (a duplicate key inside one criterion) with the built binary: `review preflight` now reports `REVIEW_READINESS_INVALID`, not the round-1 `STALE` — see Round-2 Rehearsal.
* `MEDIUM-1` (the raw Markdown appendix used only `escapeHtml`, so the hostile fixture's HTML still contained raw U+202E characters, and Sidecar content could appear twice): fixed by excluding every Readiness Sidecar path from that appendix loop entirely (`render.ts`'s `renderAppendix`, `readinessSourcePaths` exclusion set) — it is already shown, fully escaped via `escapeEvidenceProse`/`escapeEvidenceField`, in its own Story card, so removing it from the generic dump fixes the double-print and means that loop's plain `escapeHtml` never needs to guard against Sidecar bidi content at all. Test: `packages/cli/test/review-readiness.test.mjs` — `"MEDIUM-1: a within-limit Sidecar's hostile bidi content never appears in the raw Markdown appendix (shown once, only in its own Story card)"` (asserts zero raw U+202E anywhere in the page and exactly one occurrence of the escaped marker). Re-verified against the reviewer's real `hostile/` fixture with the built binary: zero raw U+202E, zero raw `<script>` tags, two escaped occurrences (one per duplicated hostile output id, not a double-print) — see Round-2 Rehearsal.
* `LOW` `readiness-digests` must still report `updated` if the post-rewrite re-index fails: fixed by attaching `{ updated: toDataValue(updated) }` to the `ERROR`/exit-3 envelope in that branch instead of an envelope with no `data` at all.
* `LOW` the symlink/unsafe-path re-check must run before staging any temp file: fixed by moving that check to its own loop over every planned rewrite, run before the staging loop begins (the redundant post-staging copy of the same check was removed; the byte-content re-check still runs immediately before renaming). Test: `packages/cli/test/review-readiness.test.mjs` — `"LOW: readiness-digests re-checks the symlink/unsafe path before staging any temp file"` (a two-Story batch where one Sidecar needs a real rewrite and the other is symlinked: nothing is written for either).
* `LOW` an unreadable Sidecar said "declared source is missing": fixed by wording that diagnostic's message differently for `observation.kind === "unreadable"` ("declared source exists but could not be read") versus a genuinely `missing` one, while keeping the same code/shape (contract §4). Test: `packages/cli/test/review-readiness.test.mjs` — `"LOW: an unreadable readiness.json reports a message describing it as unreadable, not missing"`.

## Code Review Follow-up — Round 1 (2026-09-23)

* `HIGH-1` (issue messages echoed Sidecar content): fixed by rewriting every `checkReadinessSidecarReferences` message and the `story_ref` mismatch message to name only a field path and array index. Tests: `packages/core/test/review-readiness-sidecar.test.mjs` — `"HIGH-1: a wrong story_ref never echoes the Sidecar's own (attacker-controlled) story_ref value"`, `"HIGH-1: every checkReadinessSidecarReferences message names only a field path and index, never Sidecar content"`; `packages/cli/test/review-readiness.test.mjs` — `"AC-006: a hostile, duplicated output id blocks preflight without leaking into issues, human output, or records"`, `"AC-006: a hostile story_ref never appears in the invalid-Sidecar issue message, human output, or records"`.
* `HIGH-2` (render left a raw bidi/hidden code point): fixed by reusing `escapeEvidenceProse`/`escapeEvidenceField` instead of plain `escapeHtml` — round 2 found this was only a partial fix (see `MEDIUM-1` above) and completed it.
* `HIGH-3` (an over-4-MiB Sidecar made preflight `configuration-error`/exit 2, and made index/render fail outright): fixed by exempting Readiness Sidecar paths from the generic 4 MiB per-source cap — round 2's `HIGH-1` then replaced the resulting "read it all into memory regardless of size" behavior with the size-checked, streamed-digest path.
* `MEDIUM` duplicate criterion/input ids and duplicate JSON keys, compared as lists not sets, present-but-unreadable handling, `readiness-digests` atomicity (staging, byte re-check, mode preservation, partial-rename reporting), post-rewrite fingerprint/sources, AC-005 byte-identical output, and the `readiness-digests` over-limit deviation reversal (`REVIEW_INPUT_TOO_LARGE`, not `REVIEW_READINESS_INVALID`): all fixed as recorded in this Story's git history; round 2 replaced the round-1 hand-written duplicate-key detector with `json-safety.ts` (see `HIGH-2` above) without changing any of these other fixes' behavior.
* `LOW` duplicate outputs reported once per id; `readAuthority`/`readTaskMode` issues surfaced as `STORY_*` preflight findings: fixed and unchanged by round 2.

## Evidence

* `AC-001`: pass — `packages/cli/test/review-readiness.test.mjs: a present, current, consistent readiness.json is listed in review index's data.sources with its own sha256; review render shows "Readiness Sidecar", the Sidecar's own field values (e.g. runner_worker), and its path in the published HTML; a confirmed batch with a valid Semantic Report and that Sidecar yields REVIEW_READY`
* `AC-002`: pass — `packages/cli/test/review-readiness.test.mjs: readiness-digests on a Sidecar with a stale story_md_digest rewrites both digest fields (2-space indent, trailing newline, unchanged criteria/key order), reports the rewritten path in data.updated, leaves an already-current Sidecar unwritten on a second run (data.updated: []), and afterwards review preflight against the pre-rewrite confirmation's fingerprint returns REVIEW_STALE with REVIEW_CONFIRMATION_STALE`
* `AC-003`: pass — `packages/core/test/review-readiness-sidecar.test.mjs: checkReadinessSidecarConsistency/checkReadinessSidecarReferences yield REVIEW_READINESS_STALE for a digest mismatch, REVIEW_READINESS_CRITERIA_MISMATCH for a criteria list differing from acceptance.md's ordered AC ids (by content or by order), REVIEW_READINESS_OPERATION_UNGRANTED for an ungranted operation and for a runner_worker operation outside plan/modify, REVIEW_READINESS_REFERENCE_UNKNOWN for a prerequisite_story_ref outside the dependency closure, a prerequisite_output owned outside the closure, a follow_up_story_ref outside the batch, and a duplicate outputs[].id across two Stories (reported once); human/external-owner criteria produce no operation finding. Every reference-check message names only a field path and index. packages/cli/test/review-readiness.test.mjs confirms the same REVIEW_READINESS_CRITERIA_MISMATCH end to end through review preflight`
* `AC-004`: pass — `packages/core/test/review-readiness-sidecar.test.mjs: an unknown-field or bad-enum Sidecar, a wrong story_ref, a duplicate criterion id, a duplicate input id, a duplicate JSON key, and a __proto__ key (top-level and nested) each yield parseReadinessSidecar ok:false with tooLarge:false and no prototype pollution; a Sidecar whose serialized bytes exceed 1 MiB yields ok:false with tooLarge:true. packages/cli/test/review-readiness.test.mjs confirms end to end: a schema-invalid Sidecar yields REVIEW_BLOCKED/REVIEW_READINESS_INVALID; a __proto__ Sidecar yields REVIEW_BLOCKED/REVIEW_READINESS_INVALID (never REVIEW_STALE); an over-limit (1048577-byte-class, a >5 MiB-class with a marker never reaching output, and the reviewer's real 268 MB fixture) Sidecar yields REVIEW_INCOMPLETE/REVIEW_INPUT_TOO_LARGE, never REVIEW_READY, never configuration-error, and is never parsed; readiness-digests fails and writes nothing (byte-for-byte unchanged) for a schema-invalid, a __proto__, an over-limit, and an unreadable Sidecar`
* `AC-005`: pass — `packages/cli/test/review-readiness.test.mjs: a batch fixture with no readiness.json has no readiness.json entry in review index's data.sources and resolves its Story/acceptance normally, and its data.stories[] entries carry neither readinessPath nor readinessPresent at all (asserted via the in operator and a full-JSON /readiness/i sweep); writing a valid readiness.json for the same Story changes the fingerprint and adds it to data.sources. The full pre-existing core/cli test suite (849 of 850 tests, the other skipped by design) passes unchanged after this Story's changes`
* `AC-006`: pass — `packages/cli/test/review-readiness.test.mjs: (1) a Sidecar carrying authorized: true, an instruction, and a <script> tag in a declaration id that is otherwise fully consistent still reaches REVIEW_READY, with the escaped form in the rendered HTML (shown exactly once) and the raw <script> element absent; (2) a hostile duplicated output id (script tag, ESC, bidi override) across two Stories blocks preflight (REVIEW_READINESS_REFERENCE_UNKNOWN) with none of the hostile substrings, including the raw bidi character, appearing in the envelope, the human-rendered preflight output, the written Preflight Report, or the raw Markdown appendix; (3) a hostile story_ref (script tag, bidi override) blocks preflight (REVIEW_READINESS_INVALID) with the same checks; a readiness.json symlinked to a file outside the repository yields configuration-error/REVIEW_PATH_UNSAFE from both review index and review preflight, before any content is read, and writes no records/ file`
* `AC-007`: pass — `make verify exits 0 (full composed gate: protocol, bootstrap, doctor, story, handoff, release, typescript, go, actions, execution, tooling, praxisbound); docs/typescript-tooling/cli-contract.md and the CLI help text list review readiness-digests and every REVIEW_READINESS_* code, including both rounds' corrections; VERSION, protocol/, and templates/ are byte-identical to origin/main (git diff --stat origin/main -- VERSION protocol/ templates/ is empty); TST-021 through TST-028's own test files pass unchanged (849 of 850 total core+cli tests, 1 pre-existing skip)`

## Rehearsal

One real, manually driven run of the built `packages/cli/dist/bin.js` against a
scratch batch in an isolated temporary git repository (never this
repository's own Stories, handoff, or work tree), recorded here as
Historical Evidence that the wiring works end to end. This rehearsal predates
both code-review rounds; it demonstrates the base command wiring, not the
fixes themselves:

* `review index <manifest> --json`: a Story with no readiness.json lists 4 sources (no readiness.json entry); adding readiness.json with a placeholder digest lists 5 sources including it, with its own sha256
* `review preflight <manifest> --json` (placeholder-digest Sidecar, no confirmation/report): `REVIEW_BLOCKED`, exit 1, `REVIEW_READINESS_INVALID` ("story_md_digest has an invalid form") alongside `REVIEW_CONFIRMATION_MISSING`/`REVIEW_SEMANTIC_MISSING`
* `review readiness-digests <manifest> --json` (schema-invalid placeholder): `failure`, exit 1, `REVIEW_READINESS_INVALID`, no file changed
* readiness.json edited to a well-formed but stale `sha256:00...00` digest; `review readiness-digests <manifest> --json`: `success`, exit 0, `data.updated` names the Sidecar path; the file's `story_md_digest`/`acceptance_md_digest` now equal the current `story.md`/`acceptance.md` bytes, `criteria`/`inputs`/`outputs`/`decision_follow_ups` and key order unchanged, 2-space indent, one trailing newline
* A Definition Confirmation and Semantic Report written for the post-rewrite fingerprint; `review preflight <manifest> --semantic-report semantic-report.json --json`: `REVIEW_READY`, exit 0, no diagnostics

## Round-2 Rehearsal

Real, manually driven runs of the built `packages/cli/dist/bin.js` against the
code reviewer's own fixtures (`.../scratchpad/r2/{huge,hostile,dupkey}`, never
this repository's own Stories or work tree), recorded as Historical Evidence
that the HIGH-1/HIGH-2/MEDIUM-1 fixes hold against the exact reproductions,
not only the new automated tests:

* `huge/specs/stories/AA-001-a/readiness.json` is 268,436,111 bytes. Under `node --max-old-space-size=512` (a 512 MiB heap cap, well under the file's size): `review index --json` exits 0, `success`, and reports the Sidecar's sha256 as `96b6c629…4e6a9a`, which matches `shasum -a 256` of the real file exactly (the digest was genuinely streamed from the real bytes, not fabricated); `review render --output` exits 0, `success`, produces a 101,944-byte HTML file (not hundreds of megabytes) containing 5 occurrences of the size notice text and no Sidecar content; `review preflight --json` exits 1, includes `REVIEW_INPUT_TOO_LARGE` among its issues (alongside unrelated pre-existing findings from this scratch batch's other sources) and does not crash; `review readiness-digests --json` exits 1, `failure`, with exactly one issue, `REVIEW_INPUT_TOO_LARGE`, `"readiness.json exceeds 1048576 bytes"` — no attempt to parse the file. None of the four commands allocated memory proportional to the 268 MB file under the 512 MiB cap.
* `hostile/specs/stories/AA-001-a/readiness.json` (duplicated `<script>alert(1)</script>‮` output id, an ESC sequence, a bidi-override gate id): `review render --output` produces HTML containing zero raw U+202E characters, zero raw `<script>alert` occurrences, and exactly two escaped `&lt;script&gt;alert` occurrences (one per duplicated output id — not a double-print).
* `dupkey/specs/stories/AA-001-a/readiness.json` (`"owner": "human", "owner": "integration_final"` inside one criterion — the literal round-1 HIGH-2 repro): `review preflight --json` now reports `REVIEW_READINESS_INVALID` among its issues; the round-1 bug (`STALE` only, silently accepting the duplicate) does not reproduce.

## Authority Used

* plan
* modify
* commit

## Residual Risks

* `This diff has had two code-review rounds in this session; both rounds' findings are fixed and tested above, but a third, confirming review has not run.`
* `The Readiness Sidecar's own consistency checks (digest, criteria, operations, references) validate agreement with the Story and batch only; they never verify that a declaration is true, exactly as contract §21 states.`
* `review readiness-digests re-reads and re-validates every present Sidecar on every run (no incremental cache), and re-reads the whole batch a second time after a successful rewrite to report the post-rewrite fingerprint; bounded by contract §13's 200-Sidecar-per-batch limit, but a very large batch pays for two full passes.`
* `json-safety.ts is a byte-for-byte port of goal-plan-artifacts.ts's scanJsonSafety, not a shared import, per explicit instruction (TST-029 is rewriting that file on another branch); the two copies are expected to be deduplicated after both branches merge, and will drift if one is changed without the other in the meantime.`
* `The mid-rename-failure test drives runReviewReadinessDigests through an injectable ReadinessDigestsFilesystem.rename rather than a real filesystem-level fault; this proves the code path deterministically but does not prove every real-world rename failure mode reaches it identically.`
* `The streamed sha256 path (readReadinessSidecarObservation) is new I/O code exercised by one Node-level test (3+ MiB) and one real-binary rehearsal (268 MB) in this session; it has not been fuzzed against unusual filesystem conditions (e.g. a file that grows between the fstat size check and the stream completing).`
